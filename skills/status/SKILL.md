---
description: Show wait.money client status, earnings balance, link state, and version. Use when the user asks about their wait.money earnings, whether the ad line is on, or whether the client is up to date.
---

Run `waitmoney status` and summarize the output for the user: client version, paused or active, linked account (if any), and current balance.

If `waitmoney` is not on PATH, the client is not installed; point the user to `/wait-money:install`.
